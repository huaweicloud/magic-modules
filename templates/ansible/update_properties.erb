def update_properties(module, response, array_index, exclude_output=False):
    r = user_input_parameters(module)
<% object.all_user_properties.each do |prop| -%>
<%   c = convert_resp_parameter(prop, "response, array_index", "", "r", 4) -%>
<%   unless c.empty? -%>

<%= c %>
<%   end -%>
<% end -%>

    return r
<% properties_to_show(object).each do |prop| -%>
<%= lines(build_flatten_method("", prop)) -%>
<% end -%>
